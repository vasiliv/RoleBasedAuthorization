@using Microsoft.AspNetCore.Identity
@model IEnumerable<IdentityRole>

@{
    ViewData["Title"] = "Roles";
}

<h2>Application roles</h2>
<p><a asp-action="Create">Create new role</a></p>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            <th>@Html.DisplayNameFor(model => model.Name)</th>
            <th>@Html.DisplayNameFor(model => model.NormalizedName)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>@Html.DisplayNameFor(modelItem => item.Id)</th>
                <th>@Html.DisplayNameFor(modelItem => item.Name)</th>
                <th>@Html.DisplayNameFor(modelItem => item.NormalizedName)</th>
            </tr>
        }
    </tbody>
</table>